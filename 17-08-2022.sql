create database anand;
use anand;

create table Info(id int primary key, name varchar(100));

insert into Info values (103,'Manikanth');

select * from Info;
use innovators

create table anand(id int primary key, name varchar(100));



sp_helpuser

select * from SYS.tables